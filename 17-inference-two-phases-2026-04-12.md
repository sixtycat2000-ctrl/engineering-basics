# 推理的两个阶段：为什么生成比理解慢得多

> 你问模型一个问题，它秒懂。但它逐字生成回答时，每个字都要单独算一遍。理解是并行的，生成是串行的。这个差异解释了为什么量化能让模型变快——瓶颈不是算力，是内存带宽。

---

## 为什么需要理解这个

你有没有发现：模型"想"的时候很快（几秒就读完了你一整页的 prompt），但"写"的时候很慢（一个个字往外蹦）？

这不是错觉。推理（inference）分成两个完全不同的阶段，它们的性能瓶颈完全不一样。理解这个差异，才能理解为什么量化能加速模型——以及为什么长上下文那么吃内存。

---

## 入门：一张时间线看懂两个阶段

```
用户输入 prompt          模型生成回复
┌──────────────┐        ┌──────────────────────────────┐
│              │        │token1 token2 token3 ... tokenN│
│  Prefill     │        │  ↓      ↓      ↓          ↓  │
│  一次性处理   │        │  每 次 只 生 成 1 个 token    │
│  全部 prompt │        │  串 行，一 个 一 个 来        │
│              │        │                              │
│  计算量大    │        │  计算量小，但要读全部 KV Cache │
│  但可以并行  │        │  受内存带宽限制               │
│  只发生一次  │        │  发生 N 次（N=输出长度）      │
└──────────────┘        └──────────────────────────────┘
     ~0.5 秒                       ~10-30 秒
```

---

## Prefill 阶段：一次性消化整个 prompt

你发了一个 1000 token 的 prompt。模型要做什么？

**一次性把所有 token 全喂进去，并行计算每一层的 Key 和 Value，存进 KV Cache。**

```
Prefill 处理流程：

prompt: "请帮我写一段关于量化的介绍，要求..."  (1000 tokens)
         │
         ▼
    ┌─────────┐
    │  Layer 1 │ ──► 计算 K₁, V₁ ──► 存入 KV Cache
    ├─────────┤
    │  Layer 2 │ ──► 计算 K₂, V₂ ──► 存入 KV Cache
    ├─────────┤
    │   ...    │         ...
    ├─────────┤
    │  Layer 32│ ──► 计算 K₃₂, V₃₂ ──► 存入 KV Cache
    └─────────┘
         │
         ▼
    输出第 1 个 token 的概率分布
```

关键特点：

| 特点 | 说明 |
|------|------|
| 并行计算 | 1000 个 token 同时处理，GPU 算力拉满 |
| 只发生一次 | Prompt 处理完就结束了 |
| 计算密集型 | 瓶颈是**算力**（GPU FLOPS） |
| KV Cache 填满 | 此时 KV Cache 已经占了 prompt 长度对应的全部内存 |

Prefill 阶段可以用 **Flash Attention** 等技术加速——它把注意力计算切成小块在 GPU 的快速缓存里完成，不需要在显存里存一个 N×N 的巨大中间矩阵。这个后面第 6 篇会详细讲。

---

## Decode 阶段：逐字生成，一个一个来

Prefill 结束后，模型开始生成回答。**每次只生成 1 个 token。**

每生成一个 token，模型要做：
1. 用**已有的全部 KV Cache**（包括 prompt 的 + 之前生成的）
2. 算出一个新 token 的概率分布
3. 选概率最高的那个 token
4. 把这个新 token 的 K 和 V 也存进 KV Cache

```
Decode 处理流程（每生成 1 个 token 重复一次）：

已有 KV Cache: [K₁, V₁, K₂, V₂, ..., Kₙ, Vₙ]  (N 可能 = 几千到几万)
                        │
                        ▼
                   ┌─────────┐
                   │  搬 KV  │  ← 从显存搬到计算单元
                   │  到 GPU │
                   ├─────────┤
                   │  算 1 个 │  ← 计算量很小
                   │  token  │
                   ├─────────┤
                   │  存新的  │
                   │  K, V   │
                   └─────────┘
                        │
                        ▼
              新 token: "量"  (然后重复，生成下一个)

瓶颈不是计算，是"搬数据"
```

关键特点：

| 特点 | 说明 |
|------|------|
| 串行处理 | 每次只生成 1 个 token，必须一个一个来 |
| 发生 N 次 | N = 输出长度，可能几百到几千 |
| 内存密集型 | 瓶颈是**内存带宽**（把 KV Cache 从显存搬到 GPU） |
| KV Cache 持续增长 | 每多 1 个 token，KV Cache 就多一点点 |

---

## 关键洞察：为什么 Decode 比 Prefill 慢

```
Prefill：算力瓶颈
  1000 个 token 并行算 → GPU 算力拉满 → 充分利用
  速度：~1000 tokens / 秒（取决于 GPU 算力）

Decode：带宽瓶颈
  每个 token 要读整个 KV Cache → 数据搬来搬去 → GPU 算力闲置
  速度：~30-50 tokens / 秒（取决于内存带宽）
```

**Decode 慢不是因为计算多，而是因为要频繁地从显存搬数据。** GPU 算力可能只用了 1-5%，绝大部分时间在等数据从显存传输过来。

这就像一个很聪明的学生（GPU 算力很强），但每次答题都要先翻一遍整本笔记（KV Cache），翻笔记的速度决定了他答题的速度，而不是他的脑子。

---

## 为什么量化能加速

这就解释了一个反直觉的事实：**量化完模型反而更快。**

```
❌ 直觉：参数少了 → 计算少了 → 快了
✅ 真相：参数少了 → 要搬的数据少了 → 内存带宽压力小了 → 快了
```

4-bit 模型的参数体积是 float16 的 1/4。Decode 阶段每个 token 要读的所有权重，从显存搬到计算单元的数据量减少了 75%。搬数据少了，等待时间就少了。

反量化（把整数乘回 scale 还原成浮点数）的计算量极小——一次乘法而已——比从显存搬一个 float16 快得多。

```
float16：每个 token 要搬 16 GB 权重
4-bit：  每个 token 要搬  4 GB 权重  ← 少搬 75%

实际加速效果（Qwen3.5-9B）：
  bfloat16:  19 tokens/s
  4-bit:     43 tokens/s  ← 快了 2.3 倍
```

---

## 进阶：Prefill 和 Decode 的速度差异有多大

用 `llama-bench` 实测 Qwen3.5-9B 在不同硬件上的表现：

| 阶段 | M1 Max | H100 GPU | 瓶颈 |
|------|--------|----------|------|
| Prefill (tg=1) | 快 | 很快 | 算力 |
| Decode (tg=128) | 慢 | 中等 | 内存带宽 |

H100 的算力是 M1 Max 的几十倍，但 Decode 速度只快了几倍——因为 Decode 受内存带宽限制，H100 的显存带宽优势不如算力优势那么大。

---

## 高级：投机采样——让 Decode 也并行

Decode 的串行本性是天生的——每个 token 依赖前一个。但有办法绕：

**投机采样（Speculative Decoding）**：用一个小模型快速猜几个 token，然后用大模型一次性验证。猜对了就白赚几个 token 的速度，猜错了就回退。

```
正常 Decode：         猜 1 个 → 等 → 猜 1 个 → 等 → 猜 1 个 → 等
投机采样：             小模型猜 5 个 → 大模型一次验证 → 可能赚 3-4 个

加速比：1.5x - 3x
代价：需要额外一个小模型
```

---

## 速查表

| 记住这个 | 原因 |
|---------|------|
| Prefill **算一次**，Decode **算 N 次** | Prefill 并行处理 prompt，Decode 逐字生成 |
| Decode 速度瓶颈是**内存带宽**，不是算力 | 每个 token 要读整个 KV Cache |
| 量化加速因为**搬的数据少了** | 4-bit 权重体积是 float16 的 1/4 |
| KV Cache 在 Prefill 时填满 | 后续每个 token 只追加一点点 |

---

## 接下来

知道了推理的两个阶段和 KV Cache 的作用，现在可以深入量化本身了：三种映射算法怎么把浮点数压成整数，误差从 117% 一路降到 8%。

→ 下一篇：[量化三步曲——从 16 位压到 4 位的数学](18-quantization-algorithms-2026-04-12.md)

---

## 参考资料

- [Quantization from the ground up](https://ngrok.com/blog/quantization) — 速度实测数据
- [Flash Attention 论文](https://arxiv.org/abs/2205.14135) — Prefill 加速原理
- [Speculative Decoding](https://arxiv.org/abs/2302.01318) — 投机采样加速方法
- [Transformer Inference Arithmetic](https://kipp.ly/transformer-inference-arithmetic/) — 推理算术详解

---

## 系列导航

1. [LLM 的内存账本](15-llm-memory-budget-2026-04-12.md) →
2. [浮点数精度](16-floating-point-precision-2026-04-12.md) →
3. **推理的两个阶段**（本文）→
4. [量化三步曲](18-quantization-algorithms-2026-04-12.md) →
5. [量化效果实测](19-quantization-benchmark-2026-04-12.md) →
6. [长上下文的救兵](20-long-context-2026-04-12.md)
