# 量化效果实测：4 倍压缩到底丢多少精度

> 量化完了不是拍脑袋说"感觉还行"。三个指标 + 实测数据回答：8-bit 几乎无损，4-bit 是甜点，2-bit 是悬崖。而"直接跟模型聊天"其实是最实用的质检手段。

---

## 为什么需要测

上一篇讲了三种量化算法。但"平均误差 8%"到底是什么意思？用户感知得到吗？

这篇文章用三个指标来量化回答，并给出实测数据。测试模型：**Qwen3.5-9B**。

---

## 指标一：Perplexity——模型觉得有几个"合理答案"

Perplexity 是最常用的量化质量指标。

**直觉读法：数字越小，模型越"确定"正确答案。**

- Perplexity = 1.08 → "我觉得就 1 个答案靠谱"（非常确定）
- Perplexity = 33 → "我觉得有 33 个答案都有可能"（很不确定）

怎么算的？模型给每个位置预测下一个词的概率分布，取正确答案的概率，做一点数学变换：

```
Perplexity = 1 / P(正确答案)

例子：模型预测 "2+2=?" 的下一个词
  "4"  → 92.3%  ← 正确答案
  "5"  → 3.2%
  "3"  → 1.2%

P(正确) = 0.923
Perplexity = 1/0.923 ≈ 1.08  ← 很确定
```

实际测试时用一段参考文本（通常用 wikitext-2），用滑动窗口逐个位置预测，统计所有位置的平均表现。

**实测结果（Qwen3.5-9B）**：

| 格式 | Perplexity | 变化 | 一句话评价 |
|------|-----------|------|-----------|
| bfloat16 | 8.186 | 基线 | — |
| 8-bit 对称 | 8.193 | +0.1% | 几乎没变 |
| 4-bit 非对称 | 8.563 | +4.6% | 略微退化 |
| 4-bit 对称 | 8.71 | +6.4% | 可感知退化 |
| 2-bit | 66.1 | +707% | **模型崩溃** |

⚠️ Perplexity 的局限：它只看正确答案的概率，不管其他答案怎么变。如果正确答案的概率没变，但所有错误答案的概率都变了，Perplexity 看不出来。所以需要第二个指标。

---

## 指标二：KL Divergence——两个分布"差多远"

KL Divergence 衡量量化前后模型回答的**整体概率分布**变化。

直觉：量化前后给模型同一个问题，看它给出的所有答案的概率分布整体偏了多少。

| 格式 | KL Divergence | 一句话评价 |
|------|--------------|-----------|
| 8-bit | 0.0008 | 分布几乎一样 |
| 4-bit 非对称 | 0.059 | 小幅偏移 |
| 4-bit 对称 | 0.068 | 稍大偏移 |
| 2-bit | 2.145 | 完全不同 |

KL Divergence 的局限：没有直觉含义，只能"越小越好"，而且只能对比同一个模型的不同量化版本。

⚠️ 但它比 Perplexity 更全面——Perplexity 只看正确答案，KL Divergence 看所有答案的概率分布。

---

## 指标三：基准测试 + 直接问它

**GPQA Diamond 基准**：198 道很难的物理/化学/生物选择题，4 选 1，盲猜 = 25%。

| 格式 | 正确 | 错误 | 拒答 |
|------|------|------|------|
| bfloat16 | 66.7% | 33.3% | 0% |
| 8-bit | 73.2% | 26.8% | 0% |
| 4-bit 非对称 | 62.6% | 36.4% | 1% |
| 4-bit 对称 | 66.2% | 29.3% | 4.5% |
| 2-bit | 1% | 2% | **97%** |

8-bit 比原始还好？可能是统计波动——多选题有 25% 猜对概率，样本量只有 198 题。

**"直接问它"测试**——问所有量化版本同一个问题："英格兰首都是什么？"

| 格式 | 回答 |
|------|------|
| bfloat16 | 首都是**London**。 |
| 8-bit | 首都是**London**。 |
| 4-bit 非对称 | 首都是**London**。 |
| 4-bit 对称 | 首都是**London**。 |
| 2-bit | 没有回答。推理过程陷入无限循环：重复"伦敦是英格兰的首都"上百遍 |

2-bit 的 97% 拒答不是"谦虚"——是模型陷入了循环，完全丧失了生成能力。

---

## 速度实测：量化完到底快多少

用 `llama-bench` 测 tokens/s（每秒生成多少 token）：

| 格式 | M1 Max | H100 GPU | 体积变化 |
|------|--------|----------|---------|
| bfloat16 | 19 tok/s | 107 tok/s | 基线 |
| 8-bit | 32 tok/s (+68%) | 142 tok/s (+33%) | 1/2 |
| 4-bit 非对称 | 43 tok/s (+126%) | 176 tok/s (+64%) | 1/4 |
| 4-bit 对称 | 46 tok/s (+142%) | 177 tok/s (+65%) | 1/4 |
| 2-bit | 40 tok/s (+110%) | 167 tok/s (+56%) | 1/8 |

⚠️ 2-bit 比 4-bit 慢？可能因为 2-bit 用的是不同的量化格式（K-quant），反量化更复杂。而且模型质量已经崩溃，速度没有意义。

M1 Max 上 4-bit 比 bfloat16 快 2.3 倍——不是因为算得快了，是因为**搬的数据少了 75%**（上一篇讲的内存带宽瓶颈）。

---

## 综合速查表：怎么选量化格式

| 格式 | 体积 | Perplexity 变化 | 速度提升 | 推荐场景 |
|------|------|----------------|---------|---------|
| Q8_0 | 1/2 | +0.1% | +60% | 追求质量，不在乎体积 |
| **Q4_1** | **1/4** | **+4.6%** | **+140%** | **日常使用，推荐** |
| Q4_0 | 1/4 | +6.4% | +140% | 对称版，略差 |
| Q2_K | 1/8 | +707% | ? | ❌ 不推荐 |

**甜点是 4-bit（Q4_1）**：4 倍压缩、2 倍加速、精度只丢 5%。大部分场景下用户感知不到差异。

**8-bit 几乎无损**：Perplexity 只变 0.1%，基准测试甚至可能更好。如果你不缺内存，优先用 8-bit。

**2-bit 是悬崖**：不是"不太准"，是模型彻底废了。97% 的题拒答，推理陷入循环。2-bit 对 Qwen3.5-9B 来说信息损失太大。

---

## 接下来

量化解决了权重的大小问题。但长上下文时，KV Cache 才是内存的真正杀手。最后一篇讲 KV Cache 怎么压缩——Flash Attention、TurboQuant 和 PagedAttention。

→ 下一篇：[长上下文的救兵——KV Cache 压缩与 Flash Attention](20-long-context-2026-04-12.md)

---

## 参考资料

- [Quantization from the ground up](https://ngrok.com/blog/quantization) — 完整的基准测试数据和命令
- [GPQA Diamond](https://github.com/idavidrein/gpqa) — 198 道高难度多选题基准
- [llama.cpp](https://github.com/ggml-org/llama.cpp) — 量化和评测工具

---

## 系列导航

1. [LLM 的内存账本](15-llm-memory-budget-2026-04-12.md) →
2. [浮点数精度](16-floating-point-precision-2026-04-12.md) →
3. [推理的两个阶段](17-inference-two-phases-2026-04-12.md) →
4. [量化三步曲](18-quantization-algorithms-2026-04-12.md) →
5. **量化效果实测**（本文）→
6. [长上下文的救兵](20-long-context-2026-04-12.md)
