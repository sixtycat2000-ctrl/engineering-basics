# 浮点数精度：计算机存小数的妥协艺术

> 1 和 3 之间只有 1 个整数，但有无数个小数。计算机只能存有限个，它的选择直接决定了量化能不能做。而两个巧合——浮点数在 0 附近最精确，模型参数也刚好集中在 0 附近——让量化从理论可行变成实际好用。

---

## 为什么需要理解浮点数

上一篇知道了模型大小 = 参数量 × 字节数。但凭什么能把 16 位的浮点数压成 4 位？

要回答这个问题，得先搞懂一件事：**计算机怎么存小数，以及它的精度为什么不均匀。**

因为量化的底层基础不是"少存几位就行了"，而是一个巧合：**浮点数在 0 附近精度最高，而模型的参数也刚好挤在 0 附近。**

---

## 入门：整数简单，小数麻烦

计算机用 **bit** 存数字，每个 bit 只能是 0 或 1。

存整数很直接。8 个 bit 能表示 0 到 255，一共 256 个不同的值。1 和 3 之间只有 1 个整数：2。

小数就不一样了。1.0 和 3.0 之间有多少个小数？**无穷多个**。计算机只有有限个 bit，不可能存无穷个数。

它的妥协方案：**承诺精确到 N 位有效数字，其余尽力而为。**

这就是为什么 `0.1 + 0.2 = 0.30000000000000004`——不是 bug，是妥协的代价。也是量化有误差的根源。

---

## 进阶：float32 拆开看

最常见的格式是 **float32**（单精度浮点数）。32 个 bit 分成三段：

```
┌───┬──────────┬─────────────────────┐
│ S │  E (8位)  │     M (23位)         │
├───┼──────────┼─────────────────────┤
│ ± │  指数     │    尾数              │
│   │  管范围    │    管精度            │
│   │  能存多大  │    能存多准          │
└───┴──────────┴─────────────────────┘
```

| 部分 | 位数 | 职责 | 多了怎样 | 少了怎样 |
|------|------|------|---------|---------|
| 符号 S | 1 | 正负号 | — | 只能存正数 |
| 指数 E | 8 | 数字范围 | 浪费精度 | 容易溢出 |
| 尾数 M | 23 | 有效数字 | 精度高但范围小 | 只有近似值 |

float32 的承诺：**7 位有效数字，范围 ±3.4×10³⁸。**

指数和尾数的分工像一把可伸缩的尺子：指数决定尺子的量程（能量多大），尾数决定尺子的刻度密度（能量多准）。量程和精度是 tradeoff——同一个 bit 预算里，指数多了尾数就少了。

---

## 关键发现一：精度分布不均匀

这是整个系列最重要的知识点之一。

float32 能表示大约 42 亿个不同的值。但它们的分布**不是均匀的**：

```
密 ←──────────── 0 ────────────→ 稀
| | | | | | | | | |               |                  |

-0.5 到 0.5 之间有 20 多亿个值
 10 到 20 之间只有约 100 万个值
 100万附近相邻两数差 0.125
```

为什么？因为浮点数的原理是**科学计数法**。`1.234 × 10⁵` 和 `1.234 × 10¹⁰`，尾数一样多，但后者相邻两个可表示数的间距大得多。

❌ 很多人以为：浮点数精度是均匀的，到处都一样准
✅ 实际上：**靠近 0 的地方最精确，远离 0 的地方最粗糙**

---

## 关键发现二：参数集中在 0 附近

有人下载了 6 个开源模型（Qwen3 Coder、DeepSeek R1、GLM4.7 Flash、Voxtral Mini、GPT OSS、LFM2.5），统计了所有参数的值。

结果：**99% 的参数值在 -1 到 1 之间**。

```
参数值分布（6 个开源模型汇总）

    ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
    ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
    ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
    ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓
    ▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
-3         -1         0          1          3
               ▲ 绝大多数参数在这里 ▲
```

为什么？训练时有个机制叫 **L2 正则化**，专门惩罚过大的参数值。模型被逼着把参数往小了调。这不是 bug——小参数能让模型面对没见过的问题时表现更稳定。

---

## 最关键的洞察：两个分布的"巧合"

把两个发现叠在一起：

```
浮点数的精度分布：   密 ←── 0 ──→ 稀
模型参数的值分布：   密 ←── 0 ──→ 稀
                     ▲ 完美重叠 ▲
```

**浮点数精度最高的区域，恰好是参数最密集的区域。**

这就是量化可行的物理基础。参数落在浮点数最精确的地方，所以即使减少位数，丢失的精度也相对较少。

不是巧合——正则化把参数推向 0，浮点数的科学计数法让 0 附近最密——但两个独立的设计决策恰好完美配合。

---

## 高级：从 32 位到 4 位——逐步砍精度

既然参数在精度最高的区域，能不能用更少的 bit？以下是完整对比：

| 格式 | 总位数 | 指数 | 尾数 | 有效数字 | 范围 | 每参数 | 谁在用 |
|------|--------|------|------|---------|------|--------|--------|
| float32 | 32 | 8 | 23 | ~7 位 | ±3.4×10³⁸ | 4 字节 | 传统训练 |
| float16 | 16 | 5 | 10 | ~3 位 | ±65504 | 2 字节 | 推理默认 |
| bfloat16 | 16 | 8 | 7 | ~2 位 | 与 f32 同 | 2 字节 | Google 训练 |
| float8 | 8 | 4 | 3 | ~1 位 | ±240 | 1 字节 | 实验中 |
| float4 | 4 | 2 | 1 | <1 位 | ±6 | 0.5 字节 | 极端压缩 |

**bfloat16 为什么特别？**

Google Brain 专门为 ML 设计。保持和 float32 一样宽的范围（8 位指数），精度砍到只有 2 位有效数字。

为什么？大模型训练时计算结果偶尔溢出，float16 经常遇到这个问题。bfloat16 范围够大基本不溢出，而 2 位有效数字对训练完全够用。

**float4 是什么概念？**

4 个 bit 只能表示 16 个不同的值（-8 到 7）。用 16 个刻度去近似连续的参数值，精度损失很大——但配合量化的映射技巧（下一篇讲），可以接受。

设计的 tradeoff 永远是：

```
精度（多准）
  ↑
  │  float32 ●
  │
  │  bfloat16 ●    float16 ●
  │
  │     float8 ●
  │
  │        float4 ●
  └──────────────────────────→ 范围（多大）
```

指数多 = 范围大不怕溢出，精度低。尾数多 = 精度高，但范围小。总位数少 = 省空间速度快，但两者都少。

---

## 速查表

| 记住这个 | 原因 |
|---------|------|
| 浮点数精度**不均匀**，0 附近最密 | 科学计数法的固有特性 |
| 参数**集中在 0 附近** | L2 正则化惩罚大参数 |
| 两个分布**完美重叠** | 量化可行的物理基础 |
| bfloat16 只要 **2 位有效数字**就够训练 | Google Brain 的实验验证 |

---

## 接下来

知道了浮点数的精度分布和参数分布的"巧合"，下一个问题是：LLM 推理时到底发生了什么？为什么生成第二个词比理解整个 prompt 慢得多？

→ 下一篇：[推理的两个阶段——为什么生成比理解慢](17-inference-two-phases-2026-04-12.md)

---

## 参考资料

- [Quantization from the ground up](https://ngrok.com/blog/quantization) — 浮点数可视化和参数分布统计
- [IEEE 754 浮点数标准](https://en.wikipedia.org/wiki/IEEE_754) — 浮点格式的正式规范
- [bfloat16 格式说明](https://cloud.google.com/blog/products/ai-machine-learning/bfloat16-the-secret-to-high-performance-on-cloud-tpus) — Google Brain 的设计思路

---

## 系列导航

1. [LLM 的内存账本](15-llm-memory-budget-2026-04-12.md) →
2. **浮点数精度**（本文）→
3. [推理的两个阶段](17-inference-two-phases-2026-04-12.md) →
4. [量化三步曲](18-quantization-algorithms-2026-04-12.md) →
5. [量化效果实测](19-quantization-benchmark-2026-04-12.md) →
6. [长上下文的救兵](20-long-context-2026-04-12.md)
