# LLM 的内存账本：模型到底要吃多少内存

> 80 亿参数的模型为什么占 16GB？答案是一个乘法。但这只是权重。加上 KV Cache——一个 128k 上下文的缓存可能比模型本身还大。三笔账加起来，才是模型真正需要的内存。

---

## 为什么需要算这个

你想在笔记本上跑一个 80 亿参数的模型。看了眼模型文件：4.5GB。你的 Mac 有 16GB 内存，绰绰有余，对吧？

不对。模型能跑起来需要的内存，远不止文件大小。这篇文章帮你算清三笔账。

---

## 第一笔账：权重——参数量 × 字节数

模型里绝大部分的空间被**参数（parameter）**占用。参数也叫**权重（weight）**——同一个东西，不同文章用不同名字。

参数不是抽象概念。它就是神经网络中连接两个节点的那条线上的**一个乘数**。

```
输入         参数          输出
┌──────┐   × 0.5    ┌──────┐
│ 2.0  │ ─────────► │ 1.0  │
└──────┘             └──────┘
```

一个节点乘一个参数，得到一个输出。这是神经网络的基本单元。

现代模型有几十层，每层几千个节点，每个节点和下一层的所有节点相连。层数 × 节点数 × 连接密度 = **几十亿参数**。

**核心公式**：

```
权重内存 = 参数数量 × 每个参数的字节数
```

就这么简单。一个参数占多少字节，取决于存它的格式：

| 格式 | 每参数 | 80 亿参数 | 700 亿参数 | 说明 |
|------|--------|----------|-----------|------|
| FP32 | 4 字节 | 32 GB | 280 GB | 传统训练 |
| FP16/BF16 | 2 字节 | 16 GB | 140 GB | 推理默认 |
| INT8 | 1 字节 | 8 GB | 70 GB | 8-bit 量化 |
| INT4 | 0.5 字节 | 4 GB | 35 GB | 4-bit 量化 |

**80 亿参数 × 2 字节 = 16 GB**。就是这个数。

这就是你看到的模型文件大小。90% 以上就是这些权重的体积。

---

## 第二笔账：KV Cache——随上下文长大的隐形内存

很多人不知道这笔账。它可能比权重还大。

模型在生成文本时，不会每次都重新算整个上下文。它把之前算过的中间结果存下来——这就是 **KV Cache**（Key-Value 缓存）。

为什么要存？因为**重算比存储慢得多**。用空间换时间。

KV Cache 的大小和三个东西成正比：

```
KV Cache 内存 ≈ 2 × 层数 × 上下文长度 × 隐藏维度 × 每参数字节数

2：Key 和 Value 各一份
层数：模型有几层（每层都要存自己的 KV）
隐藏维度：每层的特征向量有多大
每参数字节数：通常 FP16 = 2 字节
```

用 Llama-3-8B 举例（32 层，隐藏维度 4096）：

| 上下文长度 | KV Cache 大小 | 占比 |
|-----------|--------------|------|
| 2k tokens | ~1 GB | 权重的 6% |
| 8k tokens | ~4.3 GB | 权重的 27% |
| 32k tokens | ~17 GB | **超过权重本身** |
| 128k tokens | ~69 GB | **权重的 4 倍** |

**上下文越长，KV Cache 越大，线性增长。** 到 32k 的时候，KV Cache 已经比模型权重还大了。

```
内存占用变化（8B 模型，4-bit 权重）

  ┌─────────────────────────────────────────┐
  │                                         │ 128k context
  │          KV Cache (~69 GB)              │
  │                                         │
  ├──────┤                                  │
  │权重  │                                  │
  │(4 GB)│                                  │
  └──────┘──────────────────────────────────┘

  权重是固定的，KV Cache 随上下文线性增长
  长上下文时 KV Cache 是内存的主导者
```

**快速估算**：8B 模型 FP16，每 1k tokens 的 KV Cache 大约 **0.5-1 GB**。记住这个数，能快速估算任何配置。

---

## 第三笔账：运行时开销——不大但不能忽略

除了权重和 KV Cache，还有一些杂项：

| 开销 | 是什么 | 大约多少 |
|------|--------|---------|
| 激活值 | forward 过程中的临时张量 | 几百 MB 到几 GB |
| CUDA/框架开销 | 运行时缓冲区 | 总量的 10-20% |
| Tokenizer 等 | 辅助数据 | 几十 MB，可忽略 |

经验法则：**在权重 + KV Cache 的基础上再加 20%**，就是实际需要的总内存。

---

## 三笔账加起来：完整公式

```
总内存 ≈ 权重 + KV Cache + 20% 开销

       = (参数量 × 字节数) + (2 × 层数 × seq_len × hidden × bytes) × 1.2
```

实际例子：

| 配置 | 权重 | KV Cache (8k) | 总计 | 什么机器 |
|------|------|--------------|------|---------|
| Llama-3-8B, 4-bit, 8k ctx | 4 GB | 4.3 GB | ~10 GB | 16 GB Mac |
| Llama-3-8B, 4-bit, 32k ctx | 4 GB | 17 GB | ~25 GB | 32 GB Mac |
| Qwen-3-70B, 4-bit, 8k ctx | 35 GB | 34 GB | ~83 GB | 需要服务器 |

注意第二行：权重只有 4GB，但 32k 上下文的 KV Cache 就有 17GB。**上下文长度才是内存的真正杀手。**

---

## 入门：记住三个数

如果你只想记住三个东西：

1. **权重 = 参数量 × 字节数**。80 亿 × 2 = 16GB。
2. **KV Cache 每 1k context 约 0.5-1 GB**（8B 模型 FP16）。
3. **实际内存 ≈ (权重 + KV Cache) × 1.2**。

---

## 进阶：真实模型选型速查表

| 模型 | 参数量 | 4-bit 权重 | 8k ctx 总计 | 32k ctx 总计 | 推荐机器 |
|------|--------|-----------|------------|-------------|---------|
| Llama-3-8B | 8B | 4 GB | ~10 GB | ~25 GB | Mac 16/32GB |
| Qwen3.5-9B | 9B | 4.5 GB | ~11 GB | ~28 GB | Mac 16/32GB |
| DeepSeek-R1-32B | 32B | 16 GB | ~28 GB | ~60 GB | Mac 64GB |
| Qwen-3-70B | 70B | 35 GB | ~58 GB | ~120 GB | 服务器 |

---

## 高级：Batch Size 的影响

上面都是 batch_size = 1（一条对话）的数字。如果你同时服务多个用户：

```
KV Cache 会乘以 batch_size

batch=1, 8k ctx:  ~4.3 GB
batch=4, 8k ctx:  ~17.2 GB
batch=8, 8k ctx:  ~34.4 GB
```

这也是为什么推理服务器需要那么多 GPU 内存——不只是模型大，更是因为要同时服务很多人。

---

## 接下来

知道了模型到底需要多少内存，下一个问题：为什么量化能把参数从 16 位压到 4 位？计算机存小数到底是怎么回事？

→ 下一篇：[浮点数精度——计算机存小数的妥协艺术](16-floating-point-precision-2026-04-12.md)

---

## 参考资料

- [Quantization from the ground up](https://ngrok.com/blog/quantization) — 模型大小与量化基础
- [Efficient Large Language Models: A Survey](https://openreview.net/pdf?id=bsCCJHbO8A) — 模型效率综述
- [Transformer Inference Arithmetic](https://kipp.ly/transformer-inference-arithmetic/) — KV Cache 内存计算详解

---

## 系列导航

1. **LLM 的内存账本**（本文）→
2. [浮点数精度](16-floating-point-precision-2026-04-12.md) →
3. [推理的两个阶段](17-inference-two-phases-2026-04-12.md) →
4. [量化三步曲](18-quantization-algorithms-2026-04-12.md) →
5. [量化效果实测](19-quantization-benchmark-2026-04-12.md) →
6. [长上下文的救兵](20-long-context-2026-04-12.md)
