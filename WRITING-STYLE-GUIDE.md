# Engineering Basics 写作风格指南

> 把隐性的写作直觉变成显性的规则。读完这篇，任何主题都能写出风格一致、读者「看完就会」的技术文章。

---

## 一、核心理念：显性化 Tacit Knowledge

Michael Polanyi 说 **"We know more than we can tell"**。技术文章最大的问题不是信息不够，而是作者脑子里的隐性知识没有写出来。

什么是隐性知识？举个实际例子：

**隐性写法**（读者看完还是不会）：
```
创建 worktree 后就可以在独立目录里开发了。
```

**显性写法**（读者看完直接能做）：
```bash
cd ../my-project-feature    # 进入新 worktree 目录
git status                  # 确认显示 feature/login 分支
echo "new feature" > file.txt
git add .
git commit -m "feat: add login feature"

# 同时，原目录完全不受影响
cd ~/my-project
git status                  # 仍然显示 main 分支，干净状态
```

差异在哪？显性写法做到了三件事：
1. **可复制** —— 代码块直接复制粘贴就能跑
2. **可验证** —— 每一步都有验证手段（`git status`）
3. **可对比** —— 同时展示了"新目录"和"原目录"的状态

这就是本系列文章的设计目标：**每个读者读完后，不需要再看第二篇教程就能动手**。

---

## 二、文章结构模板

每篇文章遵循固定的骨架：

```
# 标题：一句类比或定位

> 一句话摘要（blockquote）

---

## 为什么需要 X           ← 先讲痛点/问题

## 整体架构               ← 一张图建立全局心智模型

## 核心概念/生命周期       ← 拆成有序的步骤

## 第一步：...            ← 每步有标题、有代码、有注释
## 第二步：...
## ...

## 实战/进阶              ← 完整可运行的例子

## 速查表/最佳实践         ← 一页纸能查的东西

## 参考资料               ← 延伸阅读链接
```

这个顺序不是随意排的，它遵循认知规律：

```
为什么 (动机)
  → 是什么 (全景图)
    → 怎么做 (分步骤)
      → 做出来 (完整示例)
        → 查什么 (速查表)
```

**规则：永远先讲「为什么」，再讲「怎么做」。** 读者知道为什么要用某个东西后，才会在乎怎么做。

---

## 三、七个显性化规则

### 规则 1：每个概念必须有可视形式

文字解释不清的东西，用图/表/代码三件套中的至少一个来补。

| 概念类型 | 首选表达方式 | 例子 |
|---------|------------|------|
| 架构关系 | ASCII 框图 | ACP 文章的 Client-Agent-Server 图 |
| 并列对比 | Markdown 表格 | "独立 Web App vs MCP App" 对比表 |
| 操作步骤 | 代码块 + 注释 | tmux 文章的每个操作步骤 |
| 状态流转 | ASCII 流程图 | ACP 文章的 pending → in_progress → completed |
| 布局/界面 | ASCII 布局图 | tmux 文章的分屏布局图 |
| 命令清单 | 表格 + 速查表 | Worktree 文章的命令速查表 |

**反例**：用纯文字描述架构。

```
❌ ACP 有三个角色：Client 是编辑器，Agent 是 AI 工具，MCP Server 是外部工具。
   它们通过 JSON-RPC 2.0 通信，Client 和 Agent 之间用 stdio 或 HTTP。
```

```
✅
┌─────────────┐    stdio/HTTP     ┌─────────────┐    stdio/HTTP    ┌────────────┐
│   编辑器     │ ◄── JSON-RPC ──► │  AI Agent   │ ◄── MCP 协议 ──► │ MCP Server │
│  (Client)   │                  │  (子进程)    │                  │ (工具/数据) │
└─────────────┘                  └─────────────┘                  └────────────┘
```

同一个信息，读者看文字需要 30 秒理解，看图 3 秒就懂。

---

### 规则 2：代码必须可复制粘贴直接运行

每段代码遵循「三可」原则：

**可复制** —— 代码块里只放能直接运行的命令/代码，不放伪代码：
```bash
# ✅ 正确：直接能跑
git worktree add ../my-project-feature -b feature/login

# ❌ 错误：需要读者自己替换
git worktree add <你的路径> -b <你的分支名>
```

**可验证** —— 关键步骤后加验证命令，让读者确认自己没做错：
```bash
git worktree add ../my-project-feature -b feature/login
git worktree list    # ← 验证：应该看到两个 worktree
```

**可注释** —— 注释解释「为什么」和「什么时候」，不解释代码本身：
```bash
set -g history-limit 100000    # AI 输出很长，必须够大
```

而不是：
```bash
set -g history-limit 100000    # 设置历史限制为 100000
```

---

### 规则 3：先展示问题，再给出方案

不要上来就说"用 X 很好"，而是先让读者感受到"不用 X 很痛苦"。

**模式 A：痛点列举（适合"为什么需要"段落）**
```markdown
## 为什么用 tmux

- **会话不丢** —— AI 跑几个小时，终端关了也没事
- **多任务并行** —— 左边跑 AI，右边跑测试
- **多 AI 同时用** —— 互不打扰
```

每个要点格式：**粗体关键词** —— 具体场景描述。

**模式 B：对比表（适合有替代方案的场景）**
```markdown
| 维度 | 独立 Web App | MCP App |
|------|-------------|---------|
| 上下文 | 用户切标签页 | 界面就在对话里 |
```

**模式 C：错对展示（适合设计模式/最佳实践）**
```
❌ 错误：一个 Tool 对应一个 Resource

  Tool: get_overview  → Resource: OverviewResource
  Tool: get_breakdown → Resource: BreakdownResource

✅ 正确：多个 Tool 共享一个 Resource

  Tool: get_overview  ──┐
  Tool: get_breakdown ──┼──→ Resource: DashboardResource
```

---

### 规则 4：复杂度递进 —— 每个主题分三档

```
入门（最小可用）     → 读者 5 分钟能跑起来
进阶（日常使用）     → 读者日常 80% 场景够用
高级（专家场景）     → 自动化、扩展、边缘情况
```

具体做法：

**tmux 文章的递进：**
- 入门：2 分屏（4 步搞定）
- 进阶：3 分屏（多一层）
- 高级：多 AI 并行 + tmuxinator 自动化

**Worktree 文章的递进：**
- 入门：创建 → 开发 → 合并 → 清理
- 进阶：bare 仓库 + 多 worktree 对等管理
- 高级：lock 机制 + CI/CD 保护

**ACP 文章的递进：**
- 入门：初始化 + 发一条消息
- 进阶：工具调用 + 权限管理
- 高级：会话模式 + 斜杠命令 + `_meta` 扩展

**规则：入门档的步骤必须能在 5 分钟内完成。** 如果入门就要配 10 个东西，说明拆得不够细。

---

### 规则 5：表格的四种用法

不是所有表格都一样。搞清楚每种表格的目的：

**用法 1：决策表 —— 帮读者选方案**
```markdown
| 传输 | 必须支持？ | 说明 |
|------|-----------|------|
| stdio | 是 | 默认方式 |
| HTTP  | 否 | 需声明能力 |
```
→ 读者看第一列做选择，第二列判断是否必选。

**用法 2：速查表 —— 帮读者快速查阅**
```markdown
| 快捷键 | 作用 |
|--------|------|
| `d` | 分离会话 |
| `\|` | 左右分屏 |
```
→ 读者用的时候回来翻，必须一眼能找到。

**用法 3：概念映射表 —— 把抽象概念具象化**
```markdown
| 角色 | 说明 | 例子 |
|------|------|------|
| Client | 用户界面 | Zed、JetBrains |
```
→ 三个要素：抽象名 → 一句话解释 → 具体产品名。

**用法 4：对比表 —— 帮读者理解差异**
```markdown
| 维度 | LSP | ACP |
|------|-----|-----|
| 标准化了什么 | 编辑器↔语言服务器 | 编辑器↔AI Agent |
```
→ 左列是维度（一致的比较基准），右列是具体差异。

**表格设计原则：**
- 表头不超过 4 列
- 每列内容不超过一行能看完
- 如果需要解释超过一句话，用正文不用表格

---

### 规则 6：ASCII 图的画法

ASCII 图不是装饰，是心智模型。画图的原则：

**框图（架构/组件关系）：**
```
┌─────────────┐         ┌─────────────┐
│   组件 A     │ ──────► │   组件 B     │
│             │  协议名  │             │
└─────────────┘         └─────────────┘
```
- 每个框内写组件名 +（括号里写具体产品）
- 连线上写协议/数据流名称
- 框宽对齐，视觉上干净

**流程图（状态/生命周期）：**
```
1. 初始化 (initialize)
   ├── 版本协商
   └── 能力交换

2. 创建会话 (session/new)
   └── 设置工作目录

3. 交互 (session/prompt)
   ├── 用户发消息
   ├── Agent 处理
   └── 返回结果
```
- 用缩进表示层级
- 每个节点一句话
- 关键词用代码格式或英文标注

**布局图（UI/界面）：**
```
┌──────────────────────┬──────────────┐
│                      │              │
│  AI 工具             │  测试 / git  │
│  (claude / codex)    │  (npm test)  │
│                      │              │
└──────────────────────┴──────────────┘
```
- 框内写用途 +（括号里写具体命令）
- 展示实际比例

**画图禁忌：**
- 不要画超过 60 字符宽的图（移动端会折行）
- 不要在图里写超过 8 个字的说明（放正文）
- 不要用全角字符画图（用半角 `┌─┐│└┘├┤┬┴┼`）

---

### 规则 7：注释写在代码里，不写在文字里

当代码和解释分离时，读者需要在代码和文字之间来回看。把解释直接写成代码注释：

```json
// ❌ 错误：解释在代码外面
"params": {
  "sessionId": "sess_abc123",
  "prompt": [...]
}
// 上面这个 sessionId 是之前 session/new 返回的

// ✅ 正确：解释就在旁边
{
  "sessionId": "sess_abc123",    // session/new 返回的会话 ID
  "prompt": [                    // 支持多种内容类型
    { "type": "text", "text": "帮我重构" },
  ]
}
```

**注释风格：**
- 中文注释
- `//` 风格（不是 `/* */`）
- 放在行尾（短注释）或上一行（长注释）
- 解释「为什么」「什么时候」「注意什么」，不解释代码字面意思

---

## 四、语言风格

### 句子

- **短句优先**。一句话不超过 30 字。超了就拆。
- **主谓宾结构**。不要写 "作为一种标准的协议的实现的方案"。
- **用破折号（——）补充说明**。不用括号包长句子。

### 用词

| 偏好 | 避免 |
|------|------|
| 「跑」「用」「写」 | 「运行」「使用」「编写」 |
| 「不用管」 | 「无需关注」 |
| 「直接复制」 | 「您可以将其复制」 |
| 「看下图」 | 「请参考下方图示」 |
| 「就行」 | 「即可」 |
| 「别忘」 | 「请注意务必」 |

**原则：说人话。** 读者是工程师，不需要公文腔。

### 格式标记

| 场景 | 用法 | 例子 |
|------|------|------|
| 命令/代码 | 反引号 `` ` `` | `` `git status` `` |
| 文件路径 | 反引号 | `` `~/.tmux.conf` `` |
| 概念首提 | **粗体** | **JSON-RPC 2.0** |
| 重要警告 | ⚠️ 前缀 | `⚠️ 不要让多个 AI 同时改同一个文件` |
| 错/对 | ❌ / ✅ | 见规则 3 的例子 |
| 补充说明 | `>` blockquote | 文章开头的摘要 |

### 中英文混排

- 英文前后不加空格（这是个人偏好，保持统一即可）
- 英文缩写首次出现写全称：`Agent Client Protocol (ACP)`
- 技术术语不翻译：`JSON-RPC`、`iframe`、`postMessage`

---

## 五、文章长度和深度

### 长度控制

| 类型 | 目标长度 | 判断标准 |
|------|---------|---------|
| 快速上手指南 | 200-400 行 | 读者 15 分钟内能跟着做完 |
| 完整技术指南 | 400-600 行 | 覆盖入门到进阶，读者一次读不完会收藏 |
| 深度解析 | 600+ 行 | 协议规范级别，当参考手册用 |

### 深度判断原则

问自己三个问题：

1. **读者看完能不能直接动手？** → 如果不能，加代码示例
2. **读者做错了能不能自己发现？** → 如果不能，加验证步骤
3. **读者卡住了能不能自己排查？** → 如果不能，加常见问题和注意点

三个都是「能」，深度就够了。不要为了凑篇幅加内容。

---

## 六、自检清单

写完后逐条检查：

### 结构
- [ ] 标题有一句类比/定位
- [ ] 开头有 blockquote 摘要
- [ ] 「为什么需要」在「怎么做」之前
- [ ] 有至少一张架构/流程 ASCII 图
- [ ] 有至少一个表格（速查/对比/映射）
- [ ] 结尾有参考资料链接

### 代码
- [ ] 每段代码可以直接复制粘贴运行
- [ ] 代码注释解释「为什么」不解释「是什么」
- [ ] 关键步骤有验证命令
- [ ] 没有占位符（`<your-path>` 之类）

### 内容
- [ ] 入门档步骤 5 分钟可完成
- [ ] 有 ❌/✅ 或对比表展示常见错误
- [ ] 没有只有文字没有图/表/代码的超过 10 行的段落
- [ ] 用词口语化，没有公文腔
- [ ] 句子不超过 30 字

### 文件
- [ ] 文件名格式：`topic-yyyy-MM-dd.md`
- [ ] 全中文撰写
- [ ] 用 `---` 分隔大章节

---

## 七、Tacit Knowledge 的显性化清单

以下是写这类文章时，脑子里自动做但不容易意识到的事：

| # | 隐性动作 | 显性规则 |
|---|---------|---------|
| 1 | 自动把复杂概念比作日常事物 | 标题/开头用一个类比定位主题 |
| 2 | 感觉某个地方该画图了 | 架构、流程、布局 → 必须有 ASCII 图 |
| 3 | 下意识把步骤按依赖关系排序 | 先讲被依赖的概念，后讲依赖它的 |
| 4 | 自动控制每段代码的长度 | 每段代码不超过 20 行，超了就拆 |
| 5 | 感觉某个对比需要并排看 | 并列信息 → 用表格，不用列表 |
| 6 | 下意识给代码加注释 | 注释必须回答「为什么」，不是翻译代码 |
| 7 | 自动在关键步骤后加 `git status` 之类 | 每个关键操作后加验证手段 |
| 8 | 感觉要提醒读者注意某个坑 | 用 ⚠️ 或 ❌/✅ 显式标注 |
| 9 | 自动控制信息密度 | 一个小节只讲一件事 |
| 10 | 下意识按「简单→复杂」排列选项 | 始终从最简方案开始展示 |

---

## 八、标题命名公式

标题是读者第一个看的东西，也是搜索引擎/PKM 抓取的关键。

**公式：`# 主题：一句人话定位`**

| 公式元素 | 作用 | 例子 |
|---------|------|------|
| 主题 | SEO/PKM 可检索 | `Git Worktree`、`tmux + AI Coding`、`ACP` |
| 类比/定位 | 一秒建立预期 | `AI 编程的 "USB 接口"`、`实用指南`、`完整工作流程` |

好的标题让读者在打开文章前就知道：
1. 这是关于什么的（主题）
2. 读了能获得什么（定位）

---

## 九、从隐性到显性：一篇文章的诞生过程

把写一篇文章的思考过程显性化：

```
Step 1: 确定主题
  └── 这个东西解决了什么痛点？
  └── 一句话说清楚：________

Step 2: 找类比
  └── 有什么读者已经熟悉的东西和它类似？
  └── ACP 像 USB 接口 / tmux 像指挥中心 / Worktree 像平行宇宙
  └── 类比 → 变成标题

Step 3: 画全局图
  └── 这个东西的架构/流程长什么样？
  └── 画一张 ASCII 图
  └── 图里每个角色/节点 → 后面每个是一个章节

Step 4: 拆步骤
  └── 读者按什么顺序操作？
  └── 每步：标题 + 代码 + 注释 + 验证
  └── 步骤之间有依赖关系的，按依赖排序

Step 5: 补错误和对比
  └── 读者最容易犯什么错？
  └── 加 ❌/✅ 或 ⚠️
  └── 有替代方案？加对比表

Step 6: 写速查表
  └── 把散在各处的命令/参数/概念汇总成一张表
  └── 读者用的时候回来翻这张表就够了

Step 7: 自检
  └── 按上面的自检清单过一遍
```
